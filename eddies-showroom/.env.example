# Eddie's Showroom — environment template
#
# Copy to .env.local for local dev (Vite/Next/etc.) or set these in
# Vercel Project Settings → Environment Variables.
#
# These values are also hardcoded in supabase.js for the no-build
# CDN prototype. Move them here when migrating to a real build step.

VITE_SUPABASE_URL=https://tfzxorwfafzwatmsqdxz.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_Tuv4slQxdqNaI5rzcL71VQ_PZHhrrW_

# Server-only — for Edge Functions (AI description generator, etc.)
# ANTHROPIC_API_KEY=
